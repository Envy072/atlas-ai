import type { GrowthFields } from "@/lib/business/types/synthesis";

// ARCHITECTURE ONLY. NEVER INVENT BUSINESS FACTS. Real growth drivers and
// expansion opportunities require product/market-fit signal this platform
// doesn't have yet — stays honestly empty until a future module supplies
// real input.
export function deriveGrowth(): GrowthFields {
  return { growthDrivers: [], expansionOpportunities: [] };
}
